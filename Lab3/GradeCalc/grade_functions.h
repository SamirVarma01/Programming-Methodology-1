#ifndef GRADE_FUNCTIONS_H
#define GRADE_FUNCTIONS_H

// Require functions
float calculateAverage(float grade1, float grade2, float grade3);
char determineLetterGrade(float grade);
int printGradeReport(float grade, char letter);

#endif